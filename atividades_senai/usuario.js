const prompt = require("prompt-sync")();

const nome = String(prompt("Qual seu nome?"));
const email = String(prompt("Insira seu endereço de email: "));
const idade = Number(prompt("Qual sua idade?"));
